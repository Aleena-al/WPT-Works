function MainBody() {
  return (
    <div className="bg-success">
      In your React component's JSX code, you can use the curly braces to
      interpolate the imported image file into the src attribute of the img tag.
      For example:Check for Infinite Loops or Excessive Re-renders: If your
      modifications involve changes to state or props that trigger excessive
      re-renders or infinite loops, it can cause the app to stop functioning.
      Review your code for any unintended loops or infinite update cycles.
    </div>
  );
}
export default MainBody;
